1 Шлюз - объект инкапсулрующий доступ к внешней системе или источнку данных 

когда: интерфейс внешней системы неудобен или доступ к ней осуществляется большим количеством объектов 

изоляцию от служб, которые могут изменться 

2 Преобразователь - обеспечивает взаимодействие между двемя независимы друг от друга подсистемами 

подсистемы должны быть изолированы друг от друга но между ними нужно установить взаиомдейтсвие 

3 Отделенный интерфейс
описание интерфейса размещается в одном пакете а его конкретная реализация в другом 

минимизирует взаимозависимоть между пакетами 
связанность между пакетами 

4 Объект значение -  мелкий объект, которые сравниваются по значениям егшо полей а не уникальным идентификатором DateInteraval start end set assign intersect

5 Дополнительный модуль -  позволяет определить реализацию абстракции во время выполнения программы (определяет инстанцируемый класс во время выопленния программы ппозволяет дополнять приложение новыми классами без перекомпиляции всего приложения )

6 Фиктивная служба 
Упрощенная реализация сервиса, имитирующая поведения реального компонента.

В процессе разработки и тестирования для изоляции от труднодоступных или проблемных служб. 

7 Коллекция объектов - гарантирует уникальность (единственность) каждого загруженного из БД объекта в рамках текущей сесии. или уникальность экземпляров сериализованных объектов. 

гарантирует что повторный запрос того же объекта вернет ссылку на уже существующий экземпляр в памяти а не создаст дубликат 

8 Загрузка по требованию Отложенная инициализация полей объекта до момента их непосредственного использования 